export const APP_CONFIG = {
  // Current app version. This should match the latest GitHub release tag (e.g., 'v1.1.0')
  APP_VERSION: 'v1.2.0', // Set to match the next release tag
  
  // The GitHub repository path (owner/repo)
  GITHUB_REPO: 'kunal-gswm/fintech'
};
